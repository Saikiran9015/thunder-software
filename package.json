{
    "name": "thunder-software-monorepo",
    "version": "1.0.0",
    "scripts": {
        "build": "cd web && npm install && npm run build",
        "postinstall": "cd web && npm install"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}