<UserControl x:Class="ThunderCut.Views.PouchDesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ThunderCut.Views"
             xmlns:main="clr-namespace:ThunderCut"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#121212">
    <UserControl.Resources>
        <Style x:Key="OptionButton" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="10,0">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ColorButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="15" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                             <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#1E1E1E" Padding="20,15" BorderBrush="#333333" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Click="OnBackClick" Background="Transparent" BorderThickness="0" Foreground="#00CCFF" Margin="0,0,20,0" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="←" FontSize="18" Margin="0,0,5,0"/>
                            <TextBlock Text="BACK" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Image Source="/Assets/logo.png" Height="30" Margin="0,0,15,0" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="POUCH DESIGN STUDIO" FontSize="18" FontWeight="Bold" Foreground="White"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="PREVIEW 3D" Background="#333333" Foreground="White" Padding="15,8" Margin="0,0,10,0" BorderThickness="0" Click="OnPreview3DClick"/>
                    <Button Content="FINISH &amp; PRINT" Background="#00CCFF" Foreground="Black" FontWeight="Bold" Padding="20,8" BorderThickness="0" Click="OnFinishClick"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Canvas Area -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="20">
            <Border BorderBrush="#333333" BorderThickness="1" Background="#181818" CornerRadius="10" ClipToBounds="True">
                <main:DesignCanvas x:Name="DesignArea" IsHitTestVisible="True"/>
            </Border>
            
            <!-- Context Overlay -->
            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Border Background="#CC000000" CornerRadius="20" Padding="20,10">
                    <TextBlock Text="Click on the pouch area to apply selected texture" Foreground="White" FontSize="12"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Sidebar Options -->
        <Border Grid.Row="1" Grid.Column="1" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="CUSTOMIZE LOOK" FontSize="12" FontWeight="Bold" Foreground="#666666" Margin="0,0,0,15"/>

                    <!-- Texture Categories -->
                    <Expander Header="Textures" IsExpanded="True" Foreground="White" BorderThickness="0" Margin="0,0,0,10">
                        <StackPanel Margin="0,10,0,0">
                            <Button Style="{StaticResource OptionButton}" Content="Carbon Fiber" Click="OnTextureClick" Tag="Carbon"/>
                            <Button Style="{StaticResource OptionButton}" Content="Leather" Click="OnTextureClick" Tag="Leather"/>
                            <Button Style="{StaticResource OptionButton}" Content="Matte Finish" Click="OnTextureClick" Tag="Matte"/>
                            <Button Style="{StaticResource OptionButton}" Content="Glossy" Click="OnTextureClick" Tag="Glossy"/>
                            <Button Style="{StaticResource OptionButton}" Content="Wood Grain" Click="OnTextureClick" Tag="Wood"/>
                        </StackPanel>
                    </Expander>

                    <Expander Header="Colors" IsExpanded="True" Foreground="White" BorderThickness="0" Margin="0,0,0,10">
                        <WrapPanel Margin="0,10,0,0">
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#FF0000" Click="OnColorClick"/>
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#00FF00" Click="OnColorClick"/>
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#0000FF" Click="OnColorClick"/>
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#FFFF00" Click="OnColorClick"/>
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#00FFFF" Click="OnColorClick"/>
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#FF00FF" Click="OnColorClick"/>
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#FFFFFF" Click="OnColorClick"/>
                            <Button Style="{StaticResource ColorButtonStyle}" Background="#000000" BorderThickness="1" BorderBrush="#555" Click="OnColorClick"/>
                        </WrapPanel>
                    </Expander>

                    <Expander Header="Components" IsExpanded="True" Foreground="White" BorderThickness="0">
                        <StackPanel Margin="0,10,0,0">
                            <Button Style="{StaticResource OptionButton}" Content="Add Sticker" Click="OnComponentClick"/>
                            <Button Style="{StaticResource OptionButton}" Content="Add Text" Click="OnComponentClick"/>
                            <Button Style="{StaticResource OptionButton}" Content="Upload Image" Click="OnComponentClick"/>
                        </StackPanel>
                    </Expander>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
