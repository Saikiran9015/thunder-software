<Window x:Class="ThunderCut.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ThunderCut"
        mc:Ignorable="d"
        Title="ThunderCut Studio" Height="800" Width="1200"
        Background="#121212" Foreground="White"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="0,0,1,0">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo Section -->
                <StackPanel Grid.Row="0" Margin="0,0,0,30">
                    <Image Source="Assets/logo.png" Height="80" HorizontalAlignment="Left">
                        <Image.Effect>
                            <DropShadowEffect BlurRadius="10" Color="White" Opacity="0.2"/>
                        </Image.Effect>
                    </Image>
                    <TextBlock Text="PREMIUM CUT STUDIO" FontSize="10" Foreground="#666666" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- Tools -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="DESIGN TOOLS" FontSize="10" FontWeight="Bold" Foreground="#666666" Margin="5,0,0,10"/>
                    <Button Style="{StaticResource SidebarButton}" Content="Select Tool" Tag="Select" Click="OnToolClick"/>
                    <Button Style="{StaticResource SidebarButton}" Content="Draw Paths" Tag="Draw" Click="OnToolClick"/>
                    <Button Style="{StaticResource SidebarButton}" Content="Add Cutout" Click="OnAddCutout"/>
                    <Button Style="{StaticResource SidebarButton}" Content="Text Tool" Tag="Text" Click="OnToolClick"/>
                    <Button Style="{StaticResource SidebarButton}" Content="Import SVG" Tag="Import" Click="OnToolClick"/>
                    
                    <Separator Background="#333333" Margin="0,15"/>
                    
                    <TextBlock Text="CATALOG / TEMPLATE" FontSize="10" FontWeight="Bold" Foreground="#666666" Margin="5,0,0,10"/>
                    
                    <!-- View Mode Selector -->
                    <UniformGrid Columns="2" Margin="0,0,0,10">
                        <Button x:Name="BackViewBtn" Content="BACK" Style="{StaticResource SidebarButton}" Tag="Back" Click="OnTemplateTypeClick" Background="#3300CCFF"/>
                        <Button x:Name="FrontViewBtn" Content="FRONT" Style="{StaticResource SidebarButton}" Tag="Front" Click="OnTemplateTypeClick"/>
                    </UniformGrid>

                    <!-- Scroll Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,5">
                        <Button x:Name="ScrollUpBtn" Content="▲" Width="24" Height="24" Background="#333333" BorderThickness="0" Foreground="White" Margin="2" Click="OnScrollUp"/>
                        <Button x:Name="ScrollDownBtn" Content="▼" Width="24" Height="24" Background="#333333" BorderThickness="0" Foreground="White" Margin="2" Click="OnScrollDown"/>
                    </StackPanel>

                    <TreeView x:Name="TemplateTree" Height="350" Margin="0,0,0,10" Background="Transparent" BorderThickness="0" 
                              VirtualizingStackPanel.IsVirtualizing="True">
                        <TreeView.Resources>
                            <!-- Tier Header -->
                            <HierarchicalDataTemplate DataType="{x:Type local:TierGroup}" ItemsSource="{Binding Brands}">
                                <TextBlock Text="{Binding TierName}" Foreground="#FFCC00" FontWeight="Bold" Margin="0,5"/>
                            </HierarchicalDataTemplate>
                            <!-- Brand Header -->
                            <HierarchicalDataTemplate DataType="{x:Type local:BrandGroup}" ItemsSource="{Binding Models}">
                                <TextBlock Text="{Binding BrandName}" Foreground="#AAAAAA" Margin="10,2"/>
                            </HierarchicalDataTemplate>
                            <!-- Model Item -->
                            <DataTemplate DataType="{x:Type local:PhoneModel}">
                                <Grid Margin="20,1,0,1">
                                    <TextBlock Text="{Binding Name}" Foreground="White" Opacity="0.8"/>
                                </Grid>
                            </DataTemplate>
                        </TreeView.Resources>
                        
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="False"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TreeViewItem">
                                            <StackPanel>
                                                <Border x:Name="SelectionBorder" Background="{TemplateBinding Background}" CornerRadius="4" Padding="4">
                                                    <ContentPresenter x:Name="PART_Header" ContentSource="Header"/>
                                                </Border>
                                                <ItemsPresenter x:Name="ItemsHost" Margin="5,0,0,0"/>
                                            </StackPanel>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="SelectionBorder" Property="Background" Value="#3300CCFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>
                    <Button Style="{StaticResource SidebarButton}" Content="Load Custom SVG..."/>
                </StackPanel>

                <!-- Status / Settings -->
                <StackPanel Grid.Row="2">
                    <Button x:Name="CutPreviewBtn" Background="#00CCFF" Foreground="Black" FontWeight="Bold" Padding="15,10" Click="OnCutPreviewClick">
                         GO TO CUT PREVIEW
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid x:Name="DesignView">
                <local:DesignCanvas x:Name="MainCanvas"/>
                
                <!-- Floating Controls -->
                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="20" Orientation="Horizontal">
                    <Border Background="#2A2A2A" CornerRadius="20" Padding="15,5" BorderBrush="#444444" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="-" Foreground="White" Background="Transparent" BorderThickness="0" Width="30" FontSize="18"/>
                            <TextBlock x:Name="ZoomStatus" Text="100%" VerticalAlignment="Center" Margin="10,0" Foreground="#AAAAAA"/>
                            <Button Content="+" Foreground="White" Background="Transparent" BorderThickness="0" Width="30" FontSize="18"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Preview View (Hidden by default) -->
            <ContentControl x:Name="ViewContainer" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
